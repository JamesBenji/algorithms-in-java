package com.algorithms.search;

public class BreadthFirstSearch {
    // An algorithm to solve the shortest-path problem (path with the fewest segments)
    // This is used with graph data structures. A graph is a model of connections between nodes.
    // It can help answer two types of questions:
        // Is there a path from node A to node B?
        // What is the shortest path from node A to node B?

    // A tree is a special graph where the nodes never point back up i.e, only one direction and it's down.
}
